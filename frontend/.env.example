# Base URL is proxied through Vite's dev server (see vite.config.js) so no
# VITE_API_URL is required in development. Set this only if you serve the
# frontend and backend from different origins in production.
VITE_API_URL=http://localhost:5000
